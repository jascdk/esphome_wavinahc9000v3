###############################################
# yaml_generator.yaml (info only)
#
# Manual enable/disable approach adopted:
#   Include 'yaml_generator.on.yaml' while onboarding (lots of text sensors + services)
#   Comment it out later to remove clutter. 'yaml_generator.off.yaml' is an empty stub.
#
# Example snippet for your node config:
#
# packages:
#   # Enable generator
#   # yaml_generator: !include packages/yaml_generator.on.yaml
#   # Enable debug services (optional)
#   # wavin_debug: !include packages/wavin_debug_services.yaml
#
# After copying generated YAML into your main config, just comment the line again.
# Leaving this file as documentation; safe to delete if you prefer.
###############################################

# (No active content here intentionally.)
